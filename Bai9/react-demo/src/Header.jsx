import React from 'react';
import PropTypes from 'prop-types';

// Header.propTypes = {
    
// };

function Header() {
    return (
        <div>
            Đây là Header
        </div>
    );
}

export default Header;